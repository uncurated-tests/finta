CREATE TABLE "public"."plaid_items" ("id" text NOT NULL, "created_at" timestamptz NOT NULL DEFAULT now(), "synced_at" timestamptz, "institution_id" text NOT NULL, "initial_update_complete" boolean NOT NULL DEFAULT false, "historical_update_complete" boolean NOT NULL DEFAULT false, "error" text, "access_token" text NOT NULL, "user_id" uuid NOT NULL, "sync_start_date" text, PRIMARY KEY ("id") , FOREIGN KEY ("institution_id") REFERENCES "public"."plaid_institutions"("id") ON UPDATE restrict ON DELETE restrict, FOREIGN KEY ("user_id") REFERENCES "auth"."users"("id") ON UPDATE restrict ON DELETE restrict);
