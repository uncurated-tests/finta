CREATE TABLE "stripe"."prices" ("id" text NOT NULL, "is_active" boolean NOT NULL, "product_id" text NOT NULL, "interval" text NOT NULL, "type" text NOT NULL, "unit_amount" numeric NOT NULL, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, PRIMARY KEY ("product_id") , FOREIGN KEY ("product_id") REFERENCES "stripe"."products"("id") ON UPDATE restrict ON DELETE cascade, FOREIGN KEY ("interval") REFERENCES "stripe"."intervals_enum"("name") ON UPDATE restrict ON DELETE restrict);
