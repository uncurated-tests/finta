CREATE TABLE "stripe"."plans" ("id" text NOT NULL, "is_active" boolean NOT NULL, "amount" numeric NOT NULL, "interval" text NOT NULL, "product_id" text NOT NULL, "trial_period_days" integer NOT NULL, "created_at" timestamptz NOT NULL, "updated_at" timestamptz NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("interval") REFERENCES "stripe"."intervals_enum"("name") ON UPDATE restrict ON DELETE cascade, FOREIGN KEY ("product_id") REFERENCES "stripe"."products"("id") ON UPDATE restrict ON DELETE cascade);
