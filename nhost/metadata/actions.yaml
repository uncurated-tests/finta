actions:
  - name: setupUser
    definition:
      kind: synchronous
      handler: '{{NHOST_BACKEND_URL}}/v1/functions/actions/setupUser'
    permissions:
      - role: user
    comment: Helper action to make sure user has all necessary properties
custom_types:
  enums: []
  input_objects: []
  objects:
    - name: SetupUserResponse
  scalars:
    - name: uuid
