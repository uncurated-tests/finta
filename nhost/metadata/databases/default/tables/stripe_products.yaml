table:
  name: products
  schema: stripe
select_permissions:
  - role: user
    permission:
      columns:
        - created_at
        - id
        - is_active
        - name
        - updated_at
      filter: {}
