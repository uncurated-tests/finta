CREATE TABLE "stripe"."subscriptions" ("id" text NOT NULL, "customer_id" text NOT NULL, "plan_id" text NOT NULL, "status" text NOT NULL, "cancel_at_period_end" boolean NOT NULL DEFAULT false, "current_period_start" timestamptz NOT NULL, "current_period_end" timestamptz NOT NULL, "billing_cycle_anchor" timestamptz NOT NULL, "cancel_at" timestamptz, "canceled_at" timestamptz, "started_at" timestamptz NOT NULL, "ended_at" timestamptz, "trial_started_at" timestamptz, "trial_ended_at" timestamptz, "created_at" Timestamp NOT NULL, "updated_at" timestamptz NOT NULL, PRIMARY KEY ("id") , FOREIGN KEY ("customer_id") REFERENCES "stripe"."customers"("id") ON UPDATE restrict ON DELETE cascade, FOREIGN KEY ("plan_id") REFERENCES "stripe"."plans"("id") ON UPDATE restrict ON DELETE cascade, FOREIGN KEY ("status") REFERENCES "stripe"."subscription_statuses_enum"("name") ON UPDATE restrict ON DELETE cascade);
